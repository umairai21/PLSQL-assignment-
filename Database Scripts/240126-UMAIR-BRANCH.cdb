CREATE TABLE umair_branch_tab (
    company_id NUMBER,
    branch_id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    branch_name VARCHAR2(100) NOT NULL,
    branch_address VARCHAR2(1000) NOT NULL,
    branch_manager VARCHAR2(50) NOT NULL,
    total_warehouse_capacity NUMBER NOT NULL,
    PRIMARY KEY (company_id,branch_id)
);



